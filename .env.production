# Loaded automatically by `vite build` (production mode). Committed since
# none of these values are secret — only server-side secrets are gitignored.
# Adjust VITE_API_BASE_URL to an absolute URL if Django ends up on a
# different origin than the deployed React build.
VITE_API_MODE=api
VITE_API_BASE_URL=/api
