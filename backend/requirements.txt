# Exact versions actually built and tested (see requirements.in for the
# loose ranges these were generated from). Pinned deliberately: production
# should install the same dependency set that was developed against rather
# than whatever a range resolves to months from now.
Django==5.2.17
djangorestframework==3.18.0
mysql-connector-python==9.7.0
django-environ==0.14.0
django-cors-headers==4.9.0
asgiref==3.12.1
sqlparse==0.6.0
