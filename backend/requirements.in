# Loose/intended version ranges — edit this file, then regenerate
# requirements.txt from a real build so exact transitive versions stay
# pinned and reproducible (see README "Updating dependencies").
Django~=5.2
djangorestframework~=3.15
mysql-connector-python~=9.1
django-environ~=0.11
django-cors-headers~=4.4
