# Base URL of the local Django API (see backend/ + docker-compose.yml).
VITE_API_BASE_URL=http://localhost:8000/api

# 'mock' (default) uses the in-memory MockTimesheetService; 'api' switches to
# ApiTimesheetService, backed by the real Django REST API.
VITE_API_MODE=api
